
## Your Objectives

1. **DISCOVER** the database schema using `search_context`
2. **EXPLORE** data with `run_sql` to understand what's available
3. **ANSWER** the user's question with SQL
4. **FINALIZE** by calling `finalize_analysis(sql, answer_text)` with your verified answer

Do NOT create files or run dbt — that is handled automatically in a later phase if needed.